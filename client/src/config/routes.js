const routes = {
   home: '/',
   about: '/about',
   posts: '/posts',
   post: '/post/:slug',
   questions: '/questions',
   question: '/question/:slug',
   discussions: '/discussions',
   discussion: '/discussion/:slug',
   user: '/user/:username',
   tag: '/tag/:slug',
   publish: '/publish',
   search: '/search',
   login: '/login',
   register: '/register',
   profile: '/profile',
};

export default routes;
