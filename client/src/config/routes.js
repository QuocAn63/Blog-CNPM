const routes = {
   home: '/',
   about: '/about',
   posts: '/posts',
   post: '/post/:slug',
   series: "/series",
   seriesItem: "/s/:slug",
   user: '/user/:username',
   tag: '/tag/:slug',
   publish: '/publish',
   search: '/search',
   login: '/login',
   register: '/register',
   me: "/me",
   notifications: "/notifications",
   activities: "/activities",
};

export default routes;
