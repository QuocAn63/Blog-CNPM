import React from 'react';
import styles from './Pagination.module.scss';
import classNames from 'classnames/bind';

function Pagination({ currentPage, totalPage, perPage }) {
   return <div></div>;
}

export default Pagination;
